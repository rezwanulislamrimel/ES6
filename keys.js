const glass = {
    name: 'glass',
    type: 'Glass',
    price: 12,
    isCleaned:true,
}

// console.log(glass);
const keys = Object.keys(glass);
const values = Object.values(glass);
console.log(keys);
console.log(values);